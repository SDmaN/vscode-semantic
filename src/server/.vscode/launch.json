{
    "version": "",
    "configurations": [
        {
            "name": "Attach Language Server",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build-client",
            "program": "${workspaceRoot}/src/PluginServer/bin/Debug/netcoreapp2.0/PluginServer.dll",
            "args": [],
            "cwd": "${workspaceRoot}/src/PluginServer",
            "stopAtEntry": false,
            "console": "internalConsole"
        },
        {
            "name": "Launch Language Server",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickProcess}"
        }
    ]
}